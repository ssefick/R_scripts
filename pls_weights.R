#bug.comp <- plsr.bug.red$ncomp

#larger_than <- 1/bug.comp

#choose <- c(names(plsr.bug.red$scores[,1][abs(plsr.bug.red$scores[,1])>=0.1]), #names(plsr.bug.red$scores[,2][abs(plsr.bug.red$scores[,2])>=larger_than]))